{
  "name": "@app/docs",
  "version": "0.0.0",
  "private": true,
  "license": "MIT",
  "scripts": {
    "build": "preact build --no-prerender --dest dist",
    "clean": "rimraf dist",
    "dev": "preact watch",
    "serve": "preact watch",
    "start": "preact build && preact serve"
  },
  "dependencies": {
    "@bcase/core": "^0.0.20",
    "preact": "^8.5.2",
    "preact-compat": "^3.17.0",
    "preact-render-to-string": "^4.1.0",
    "preact-router": "^2.5.7"
  },
  "devDependencies": {
    "preact-cli": "^2.1.0",
    "preact-cli-plugin-fast-async": "^1.0.1",
    "preact-cli-plugin-typescript": "^0.2.2",
    "rimraf": "^3.0.0",
    "typescript": "^3.6.3"
  }
}
